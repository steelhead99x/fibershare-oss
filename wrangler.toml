# Cloudflare Pages project for the FiberShare community site.
# Node 20: use wrangler v3 (wrangler v4 requires a newer Node).
# CLI: npx wrangler@3 pages deploy dist --project-name=fibershare-dev
name = "fibershare-dev"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"
